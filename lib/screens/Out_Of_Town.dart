import 'package:flutter/material.dart';

class Out_Of_Screen extends StatelessWidget {
  const Out_Of_Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('로고'),
      ),
      body: Text('시외셔틀'),
    );
  }
}
