import 'package:flutter/material.dart';

class DownTownScreen extends StatelessWidget {
  const DownTownScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('로고'),
      ),
      body: Text('시내셔틀'),
    );
  }
}
